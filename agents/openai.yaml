interface:
  display_name: "EA Feedback"
  short_description: "Audit EA runs and submit feedback safely"
  default_prompt: "Use $ea-feedback to review recent EA usage from active local evidence, merge duplicate findings, generate a developer-facing report, and submit it only if I authorize submission; check external channel readiness only at that submission step."
